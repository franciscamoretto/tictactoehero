Buffied Millenium update. version (2.1)


alt0176 will produce a slightly different 'a'
alt0177 will produce a slightly different 'f'
alt0178 will produce a slightly different 's'
alt0179 will produce a slightly different 'o'
alt0180 will produce a slightly different 'u'
alt0181 will produce a slightly different 'e'

added a few more characters. Changes mostly cosmetic
in the international characters. Have added hash key
character.